{
    "name": "demo",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "dev": "nodemon --watch ../../dist/*.js -x 'cross-env DEBUG=vite-plugin-swagger2ts:* vite'",
        "build": "vue-tsc --noEmit && vite build",
        "preview": "vite preview"
    },
    "dependencies": {
        "vue": "^3.2.31"
    },
    "devDependencies": {
        "@vitejs/plugin-vue": "^2.2.4",
        "cross-env": "^7.0.3",
        "nodemon": "^2.0.15",
        "typescript": "^4.6.2",
        "vite": "^2.8.6",
        "vite-plugin-restart": "^0.1.1",
        "vite-plugin-swagger2ts": "workspace:*",
        "vue-tsc": "^0.29.8"
    }
}
